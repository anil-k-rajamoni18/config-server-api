greet:
  message: Hello , Am from test service.


server:
  port: 1122
  

eureka:
  instance:
    lease-renewal-interval-in-seconds: 30
    lease-expiration-duration-in-seconds: 30
  client:
    lease.duration: 30
    eureka-server-port: 8761
    register-with-eureka: true
    fetchRegistry: true



management:
  endpoints:
    web:
      exposure.include: *
